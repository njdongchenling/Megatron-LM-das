name: docker-image

on:
  workflow_dispatch:
  schedule:
    # 每天晚上 12 点（北京时间），GitHub cron 使用 UTC 时间
    - cron: "0 16 * * *"

env:
  HARBOR_SITE: 10.16.1.152:5000
  HARBOR_NAMESPACE: jenkins/model_test_env

jobs:
  build:
    # 需要能访问内网 pip 源 / AICC 下载地址的自托管 runner
    runs-on:
      - self-hosted
      - hcu
      - hcu-ci-pr
      - nmz36
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: 获取最新 AICC
        run: |
          AICC_URL="http://10.16.1.201:8000/ai_cc/nightly/"
          LATEST_AICC=$(curl -s "${AICC_URL}" | grep -oP 'dtk_llvm_[^"]+\.run' | sed 's/\.run$//' | sort -t '_' -k4 -nr | head -n1)
          echo "AICC_URL=${AICC_URL}${LATEST_AICC}.run" >> "$GITHUB_ENV"
          echo "AICC_VERSION=${LATEST_AICC#dtk_llvm_}" >> "$GITHUB_ENV"

      - name: 生成镜像 tag
        run: |
          BUILD_TIME=$(date +"%Y%m%d-%H%M")
          echo "IMAGE_TAG=megatron:0.18.2-ubuntu22.04-dtk2604-py3.10-${BUILD_TIME}" >> "$GITHUB_ENV"
          echo "LATEST_TAG=megatron:0.18.2-latest" >> "$GITHUB_ENV"

      - name: Build
        id: build_image
        run: |
          docker build --no-cache --network=host \
            --build-arg AICC_URL="${{ env.AICC_URL }}" \
            --build-arg AICC_VERSION="${{ env.AICC_VERSION }}" \
            --build-arg RCCL_TEST="${{ vars.RCCL_TEST }}" \
            -t "${HARBOR_SITE}/${HARBOR_NAMESPACE}/${IMAGE_TAG}" \
            -f docker/Dockerfile .

      - name: 打两个 tag
        id: tag_image
        run: |
          docker tag "${HARBOR_SITE}/${HARBOR_NAMESPACE}/${IMAGE_TAG}" "${HARBOR_SITE}/${HARBOR_NAMESPACE}/${LATEST_TAG}"

      - name: Login to Harbor
        run: |
          echo "${{ secrets.HARBOR_PASSWORD }}" | docker login "${HARBOR_SITE}" -u "${{ secrets.HARBOR_USERNAME }}" --password-stdin

      - name: Push
        run: |
          docker push "${HARBOR_SITE}/${HARBOR_NAMESPACE}/${IMAGE_TAG}"
          docker push "${HARBOR_SITE}/${HARBOR_NAMESPACE}/${LATEST_TAG}"

      - name: 删除本次构建镜像
        if: always() && steps.build_image.outcome == 'success'
        run: |
          docker rmi "${HARBOR_SITE}/${HARBOR_NAMESPACE}/${IMAGE_TAG}" || true

      - name: 删除本次滚动 tag
        if: always() && steps.tag_image.outcome == 'success'
        run: |
          docker rmi "${HARBOR_SITE}/${HARBOR_NAMESPACE}/${LATEST_TAG}" || true
